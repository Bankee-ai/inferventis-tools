{
  "name": "bank_data",
  "version": "1.0.0",
  "pricingTier": "premium",
  "description": "Bank data tool. Gets account info and transactions from bank APIs.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "account_id": { "type": "string", "description": "Account ID" },
      "limit": { "type": "number", "description": "Limit" }
    },
    "required": []
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "transactions": { "type": "array" }
    }
  }
}
