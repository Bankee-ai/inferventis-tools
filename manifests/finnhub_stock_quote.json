{
  "name": "finnhub_stock_quote",
  "version": "1.0.1",
  "pricingTier": "standard",
  "description": "Retrieves real-time stock price quotes and company information for any publicly traded company via the Finnhub API. Returns current price, intraday high and low, percentage change from previous close, previous close price, sector, and exchange. Use this tool when an agent needs to look up a stock price, check intraday market performance, retrieve company sector data, monitor equity portfolio values, or answer any question about the current trading price of a publicly listed company. Do not use this tool for cryptocurrency prices — use crypto_price instead, which supports BTC, ETH, and 10,000+ digital assets. Do not use for fiat currency conversion — use currency_convert. Requires a Finnhub API key to be configured on the server.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "symbol": {
        "type": "string",
        "description": "Stock ticker symbol in uppercase. Examples: AAPL (Apple), MSFT (Microsoft), TSLA (Tesla), GOOGL (Alphabet), AMZN (Amazon), NVDA (Nvidia). Must be a valid exchange-listed ticker symbol."
      },
      "include_company_info": {
        "type": "boolean",
        "description": "Whether to include company name, sector, and exchange details alongside the price quote. Defaults to true. Set to false for a faster, price-only response."
      }
    },
    "required": ["symbol"]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "symbol": { "type": "string", "description": "The ticker symbol queried." },
      "company_name": { "type": "string", "description": "Full legal company name, e.g. 'Apple Inc.'. Present only when include_company_info is true." },
      "current_price": { "type": "number", "description": "Current trading price in the stock's native currency." },
      "change": { "type": "number", "description": "Absolute price change from the previous close." },
      "change_percent": { "type": "number", "description": "Percentage price change from the previous close. Negative values indicate a decline." },
      "high_today": { "type": "number", "description": "Intraday high price." },
      "low_today": { "type": "number", "description": "Intraday low price." },
      "previous_close": { "type": "number", "description": "Closing price from the previous trading session." },
      "sector": { "type": "string", "description": "Industry sector of the company, e.g. 'Technology'. Present only when include_company_info is true." },
      "currency": { "type": "string", "description": "ISO 4217 currency code for all price fields, e.g. USD." },
      "timestamp": { "type": "string", "description": "ISO 8601 timestamp of when the quote was fetched." },
      "data_source": { "type": "string", "description": "Always 'Finnhub'." }
    }
  }
}
