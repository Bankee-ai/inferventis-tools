{
  "name": "echo_structured",
  "version": "1.0.0",
  "pricingTier": "free",
  "description": "Transforms an input message to uppercase and returns it with a server timestamp and optional metadata passthrough. Use this tool when you need to verify MCP connectivity, test round-trip latency, or validate that structured input and output schemas are working correctly.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "message": {
        "type": "string",
        "description": "The message string to transform"
      },
      "metadata": {
        "type": "object",
        "description": "Optional key-value metadata to echo back"
      }
    },
    "required": ["message"]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "result": { "type": "string" },
      "timestamp": { "type": "string" },
      "metadata": { "type": "object" }
    }
  }
}
