{
  "name": "stripe_payments",
  "version": "1.0.1",
  "pricingTier": "premium",
  "description": "Retrieves live payment data from a Stripe account via the Stripe API. Supports four query types: recent_payments returns the latest payment intents with status, amount, and currency; failed_charges returns declined or failed charges with failure reasons and error codes; customers returns customer records with name, email, and payment method details; subscriptions returns active and cancelled subscription plans with billing interval and status. Use this tool when an agent needs to investigate payment failures, audit recent transaction activity, retrieve customer billing records, or check subscription status within a Stripe account. Do not use for bank account transactions or PSD2 Open Banking data — use open_banking_transactions instead. Requires a Stripe secret key to be configured on the server.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "query_type": {
        "type": "string",
        "description": "The type of Stripe data to retrieve. Must be one of: recent_payments (latest payment intents), failed_charges (declined or failed charges with failure reasons), customers (customer records with contact and payment method info), subscriptions (billing subscriptions with status and plan details)."
      },
      "limit": {
        "type": "number",
        "description": "Number of records to return. Integer between 1 and 25. Defaults to 10."
      }
    },
    "required": ["query_type"]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "query_type": { "type": "string", "description": "The query type that was executed." },
      "records": { "type": "array", "description": "Array of Stripe objects. Structure varies by query_type: payment intents include id, amount, currency, status, and created timestamp; failed charges add failure_code and failure_message; customers include id, email, name, and created; subscriptions include id, status, plan name, interval, and current_period_end." },
      "total_returned": { "type": "number", "description": "Number of records included in this response." },
      "data_source": { "type": "string", "description": "Always 'Stripe'." },
      "timestamp": { "type": "string", "description": "ISO 8601 timestamp of when the data was fetched." }
    }
  }
}
