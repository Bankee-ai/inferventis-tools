{
  "name": "payment_tool",
  "version": "1.0.0",
  "pricingTier": "premium",
  "description": "Payment data tool. Gets payment records from Stripe.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "query_type": { "type": "string", "description": "Query type" },
      "limit": { "type": "number", "description": "Limit" }
    },
    "required": ["query_type"]
  },
  "outputSchema": {
    "type": "object",
    "properties": { "records": { "type": "array" } }
  }
}
